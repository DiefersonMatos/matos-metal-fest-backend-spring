/**
 * This package defines the boundary interfaces for user-related business logic.
 * <p>
 * It contains service interfaces that declare operations for managing users,
 * acting as an abstraction layer between the controller layer and the business logic implementation.
 *//**
 * * This package defines the boundary interfaces for user-related business logic.
 * * <p>
 * * It contains service interfaces that declare operations for managing users,
 * * acting as an abstraction layer between the controller layer and the business logic implementation.
 */
package com.matos.business.user.boundary;
